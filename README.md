# Currency Converter API (FastAPI) <img width="40" height="40" alt="image" src="https://github.com/user-attachments/assets/992fe6e3-62e5-4db7-a7f9-7b01e6ecd02b" /> 
API-сервис для конвертации валют, построенный на FastAPI. Проект включает продвинутую систему аутентификации (JWT), фоновые задачи через Celery и интеграцию с внешним API курсов валют.

## Основной функционал
1. __Аутентификация__
   * __Регистрация__: Валидация уникальности юзернейма и сложности пароля. Данные хранятся в _PostgreSQl_.
   * __JWT Auth (Access + Refresh)__: Полноценный цикл авторизации - при успешном входе пользователь получает _Access_ и _Refresh_ токены для доступа к функционалу приложения.
   * __Обновление токенов__: Возможность получить новую пару токенов через валидный _Refresh_ токен.
   * __Blacklist для refresh-токенов__: После успешных операций логаута и обновления токенов, старый _Refresh_  попадает в базу "просроченных" токенов.
   * __Контроль доступа__: Защищенные эндпоинты (валидный _Access_ токен в заголовке); эндпоинты, доступные только админиу (поле _is_admin_ - настраивается в базе данных).

2. __Работа с валютами (доступна только авторизованным пользователям)__ 
   * __Фоновое обновление__: _Celery-worker_ раз в 2 часа забирает свежие курсы с _https://currencyapi.net_ и кэширует их в JSON-файл для оптимизации работы приложения.
   * __Возможности__: Справочник валют, актуальные курсы к USD, конвертация.

3. __Обработка исключений__
   * __Auth__: ошибки при регистрации (пользователь уже существует, слабый пароль), неверные реквизиты входа, попытка доступа к админ-функцииям без соотвествующих прав.
   * __JWT__: строгая проверка на тип токена (Access/Refresh), истечение срока действия (Expired), невалидная подпись или использование отозванного (Revoked) токена.
   * __Currency__: обработка неверных кодов валют.
  
4. __Автоматизация__: 
   * Celery-задача автоматически удаляет из _PostgreSQL_ просроченные _Refresh_ токены (согласно значенияю `REFRESH_TOKEN_EXPIRE_DAYS`) , предотвращая "раздувание" базы.

5. __Тестирование__:
   * Приложение покрыто тестами: модульные тесты для аутентификации/действия с юзерами и мокинг-тесты для взаимодействия с внешним API.
  
## Стек технологий
* Backend: _Fastapi, Uvicorn, Pydantic v2_
* Database: _PostgreSQL, SQLAlchemy, Alembic_
* Broker: _Redis_
* Tasks: _Celery, Celery Beat_
* Security: _JWT (PyJWT), Passlib (bcrypt)_
* Templates: _Jinja2_
* Frontend: _HTML, CSS_
* Logging: _Loguru_
* Testing: _Pytest, Pytest-Asyncio, Unittest Mocks_


## Структура проекта
```bash
.ConverterAPI
├── app
│   ├── api
│   │   ├── __init__.py
│   │   ├── endpoints/
│   │   ├── schemas/
│   ├── core/
│   ├── database/
│   ├── dependencies/
│   ├── exceptions/
│   ├── handlers/
│   ├── json/
│   ├── middlewares/
│   ├── services/
│   ├── tasks/
│   ├── templates/
│   └── utils/
│   ├── main.py
│   ├── __init__.py
├── logs/
├── tests/
├── .dockerignore
├── .gitignore
├── .env
├── .env.docker
├── alembic.ini
├── docker-compose.yaml
├── Dockerfile
├── pytest.ini
├── requirements.txt

```
### Директории и файлы
* `app`: Верхний уровень, содержит основной код приложения.
    * `api`: Слой интерфейса приложения.
      * `endpoints`: API-эндпоинты (роуты) для обработки HTTP-запросов.
      * `schemas`: Pydantic-схемы для валидации и сериализации данных.
    * `core`: Cодержит глобальные настройки конфигурации (Pydantic settings) и настройки безопасности (JWT).
    * `database`: Файлы миграций Alembic, конфигурация асинхронного подключения к базе данных, классы моделей SQLAlchemy.
    * `dependencies`: Зависимости (Dependencies) для проверки JWT-токенов и прав доступа путем извлечения текущего пользователя из заголовка (`Authorization: Bearer <token>`).
    * `exceptions`: Классы кастомных user-friendly исключений.
    * `handlers`: Перехват ошибок (например, 404, 500) и возврат структурированных JSON-ответов.
    * `json`: Ответ от запроса к внешнему API в формате json. 
    * `middlewares`: Промежуточное ПО для логирования запросов и ответов.
    * `services`: Бизнес-логика приложения: CRUD-операции, взаимодействие с БД.
    * `tasks`: Конфигурация Celery и Celery Beat, реализация фоновых процессов (удаление аннулированных токенов из базы данных, запрос к внешнему API).
    * `templates`: HTML-шаблон лендинговой страницы.
    * `utils`: Вспомогательные функции для подготовки данных (кастомизация списка валют, извлечение нужных данных).
    * `main.py`: Корень проекта для запуска FastAPI-приложения.
* `logs`: Файлы с логами разных уровней (INFO, WARNING, ERROR).
* `tests`: Тесты эндпоинтов, фикстуры и вспомогательные функции.
* `.dockerignore`: Список файлов и директорий, игнорируемых при сборке Docker-контейнера.
* `.gitignore`: Список файлов и директорий, игнорируемых системой контроля версий.
* `.env`: Хранение конфиденциальной информации (пароли баз данных, секретные ключи).
* `env.docker`: Хранение переменных окружения для Docker-контейнеризации.
* `alembic.ini`: Конфигурационный файл инструмента миграции баз данных Alembic.
* `docker-compose.yaml`: Файл Docker Compose, содержащий инструкции, необходимые для запуска и настройки сервисов.
* `Dockerfile`: Набор инструкций для автоматической сборки образа Docker.
* `pytest.ini`: Конфигурационный файл для pytest.
* `requirements.txt`: Список зависимостей проекта.

## Установка и запуск 
### Локальный запуск
  * __Требования__: перед запуском приложения убедитесь, что у вас установлены следующие компоненты:
      * Python 3.10+
      * PostgreSQ: заранее создайте базы данных: основную и для тестов.
      * Redis: запущенный сервис для работы Celery.
        
  1. __Клонирование репозитория__:
      
      ```bash
       git clone https://github.com/Keka17/Currency_Converter.git
       ```
  2.  __Переход в директорию проекта__:
  
         ```bash
         cd Currency_Converter
        ```
     
  3. __Создание и активация виртуальной среды__:
     
     ```bash
        python3 -m venv venv
  
        venv\Scripts\activate (Windows)
  
        source venv/bin/activate (MacOS, Linux)
     ```
    
  4. __Установка зависимостей__:
     
      ```bash
        pip install -r requirements.txt
      ```
  5. __Настройка переменных окружения__:
     
      Cоздайте файл `.env` в корне проекта, заполнив его в соответствии с файлом `.env.example`.
     
  6. __Запуск приложения__:
     
     Для полноценной работы нужно запустить три компонента:
       * __API сервер__: из корневой папки
      
      ```bash
      uvicorn app.main:app --reload
      ```
       * __Celery Worker__:
      
      ```bash
      celery -A app.tasks.celery_app.celery_app worker --loglevel=info
      ```
       * __Celery Beat__:
      
      ```bash
      celery -A app.tasks.celery_app.celery_app beat --loglevel=info
      ```

### Запуск через Docker

__Примечание__: этот способ запуска __быстрее__, т.к. не требует отдельной установки PostgreSQL и Redis. Убедитесь, что у вас установлен __Docker__ и __Docker Compose__.

1. __Клонирование репозитория__:
   
    ```bash   
      git clone https://github.com/Keka17/Currency_Converter.git
    ```
2.  __Переход в директорию проекта__:

       ```bash
       cd Currency_Converter
      ```

3. __Настройка переменных окружения__:
   
    Cоздайте файл `.env.docker` в корне проекта, заполнив его в соответствии с файлом `.env.example`.

4. __Сборка и запуск__:
   
   ```bash
    docker-compose --env-file .env.docker up --build
    ```
   

### Доступ к приложению
* После успешного запуска API будет доступно по адресу: http://localhost:8000/.
* После запуска сервера доступна __интерактивная документация__ с описанием эндпоинтов и возможностью проверить функционал:
   * __Swagger__: http://localhost:8000/docs
   * __Redoc__: http://localhost:8000/redoc

### __Примечание__: 
#### Тестирование авторизации (Swagger UI):
1. __Не используйте__ эндпоинт `/auth/login` вручную для авторизации, он предназначен только для выдачи JSON-ответа.
2. Для тестирования защищенных эндпоинтов необходимо зарегестрироваться, а затем пройти аутентификацию через OAuth2-клиент: с помощью кнопки __Authorize__ вверху страницы.  Swagger сохранит `access_token` и будет сам подставлять его в заголовки.

## Эндпоинты
* ### Users (protected)
  * `GET /users/user_info`: Получение информации о текущем пользователе.
  * `GET /users/list`: Получение списка всех пользователей, доступно только админу.
  * `DELETE /users/{user_id}`: Удаление конкретного пользователя, доступно только админу.

* ### Currency (protected)
  * `GET /currency/list`: Получение списка доступных для конвертации валют с расшифровкой ISO-кодов. Параметр запроса `code` вернет расшифровку названия валюты.
  * `GET /currency/actual_rates`: Получение актульных курсов для всех доступных валют. Параметр запроса `code` вернет актуальный курс для конкретной валюты.
  * `POST /currency/converter`: Конвертация валют.

* ### Authentication
  * `POST /auth/register`: Регистрация нового пользователя.
  * `POST /auth/login`: Аутентификация для получения пары токенов.
  * `POST /auth/refresh`: Обновление пары токенов. В заголовках (`x-refresh-token`) обязательно указывать refresh-token.
  * `POST /auth/logout`: Выход из системы. В заголовках (`x-refresh-token`) обязательно указывать refresh-token.

* ### Frontend
  * `GET /`: лендинговая страница. 


## Тестирование API c pytest

Для запуска тестов в корневой папке выполните команду:

```bash
pytest
```

## Кастомизация внешнего API

Если вы планируете использовать другой сервис, убедитесь, что:
1. Внешнее API возвращает данные в формате __JSON__.
2. Функция `get_actual_rates_data` (`app/utils/actual_rates.py`), отвечающая за парсинг ответа, __обязана__ возвращать словарь со следующией структурой:
   * Ключ `"updated"`: дата последнего обновления данных.
   * Остальные ключи: ISO-код валют, где значением является курс к USD (float).

    __Пример ожидаемой структуры__:
    
    <img width="388" height="112" alt="Expected json" src="https://github.com/user-attachments/assets/2c3b8e82-5fe0-4a5f-8d03-6e03a77bc758" />

## Примеры запросов (Postman)

* `GET /currency/list`

<img width="854" height="578" alt="currencies list" src="https://github.com/user-attachments/assets/ce536d4d-ea63-4187-91d1-7a375df0a9a7" />


* `GET /currency/list?code=brl&code=NGN`

<img width="822" height="393" alt="specific currencies list" src="https://github.com/user-attachments/assets/1b4c5144-e065-4c63-b208-aaa4b88cabd5" />


* `GET /currency/actual_rates`
  
  <img width="848" height="598" alt="actual rates" src="https://github.com/user-attachments/assets/26d16d6f-b826-48fc-8330-e161425b136b" />


* `GET /currency/actual_rates?code=CZK&code=JPY`

<img width="771" height="420" alt="actual rates specific" src="https://github.com/user-attachments/assets/ea9e9915-2b6d-4de4-ba0e-ac257a2c357d" />


* `POST /currency/converter`
  
 <img width="650" height="409" alt="converter" src="https://github.com/user-attachments/assets/dcd1f228-b30c-403c-8fe9-d12649d540cf" />


## 🙏 Благодарности
Данное приложение было разработано в качестве первого финального проекта по завершении курса **["Быстрый старт в FastAPI Python"](https://stepik.org/course/179694/info)** на платформе **Stepik**. 

Выражаю благодарность автору за структурированные основы и отличный практический фундамент, который помог мне реализовать этот проект.
